import Header from '../components/Header';

export default function Board() {
    return (
        <>
            <Header />
            <h1>Boards will be the main profile page</h1>
            <p>Figure out how to save pins to different boards</p>
            <p>then this will be the Board Index Page</p>
        </>
    )
}